mode: ContinuousDeployment
branches:
  develop:
    regex: ^develop$
    label: dev
    increment: Patch
  master:
    regex: ^master$
    label: ''
    increment: Patch
  main:
    regex: ^main$
    label: ''
    increment: Patch

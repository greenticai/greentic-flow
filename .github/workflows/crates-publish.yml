name: Publish greentic-flow (crate-only)
on:
  push:
    tags: ["flowcrates-v*"]
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  publish:
    uses: greenticai/.github/.github/workflows/crates-publish.yml@main
    with:
      crates: "greentic-flow"
    secrets: inherit
